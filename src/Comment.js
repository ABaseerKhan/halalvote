class Comment {
  constructor(username, post, replies, upVotes, downVotes) {
    this.username = username;
    this.post = post;
    this.replies = replies;
    this.upVotes = upVotes;
    this.downVotes = downVotes;
  }
}

export default Comment;
